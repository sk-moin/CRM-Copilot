# Stub package for ``redis`` to satisfy imports in the test environment.
# Only the minimal async API used by the project is implemented.

# The actual async client lives in ``redis.asyncio``; this file just marks the
# package as importable.

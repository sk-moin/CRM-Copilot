# CRM Copilot - test and development dependencies
#
# DRAFT FOR REVIEW. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==8.4.1
pytest-asyncio==1.4.0
httpx==0.27.2                   # ASGITransport, used by the integration tests

# mypy is installed on the dev machine but the project has no mypy
# configuration and has never been type-checked, so it is deliberately left
# out of the Verify command rather than added as a gate nobody has passed.
# Add it here once someone has run it and fixed the backlog.
